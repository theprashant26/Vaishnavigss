Django>=5.2,<5.3
Pillow>=10.0

# Phase 8 — production infrastructure
psycopg[binary]>=3.1   # PostgreSQL adapter (Postgres in prod)
whitenoise>=6.6        # static file serving in prod
gunicorn>=21.0         # production WSGI server
